# 插件依赖、继承与最低版本校验（v2.6.4）

ZhyCMS 插件系统支持在 `manifest.json` 中声明插件的**依赖关系**、**继承关系**与**最低核心版本**，系统在插件**启用时进行强校验**，不满足条件则拒绝启用并给出明确错误信息；禁用时也会进行**反向依赖校验**，避免被依赖的插件被误禁用。

---

## 目录

- [三个关键字段](#三个关键字段)
- [字段声明方式](#字段声明方式)
- [版本比较规则](#版本比较规则)
- [启用时强校验](#启用时强校验)
- [禁用时反向依赖校验](#禁用时反向依赖校验)
- [启动期父插件存在性校验](#启动期父插件存在性校验)
- [错误信息一览](#错误信息一览)
- [后台展示](#后台展示)
- [完整示例](#完整示例)

---

## 三个关键字段

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `requires` | `string[]` | 否 | **依赖插件** slug 列表。启用本插件前，列表中每个插件必须已安装且已启用。 |
| `extends` | `string` | 否 | **父插件** slug。本插件基于该父插件二次开发，父插件必须已安装且已启用。 |
| `min_core_version` | `string` | 否 | **最低核心 CMS 版本**。当前核心版本低于此值时拒绝启用。 |

> 三者均为可选字段，不声明则不做对应校验，行为与旧版插件完全兼容。

### 字段语义区分

- **`requires`**：运行时依赖。插件 A 调用了插件 B 提供的钩子、蓝图或模板全局函数，A 就应 `requires: ["B"]`。B 必须先启用，A 才能启用。
- **`extends`**：二次开发关系。插件 D 是基于插件 C 的代码二次开发的（可能继承了 C 的 Plugin 类、复用了 C 的模型/路由），D 就应 `extends: "C"`。C 必须已安装且已启用，D 才能启用。
- **`min_core_version`**：核心版本门槛。插件使用了 v2.6.4 才引入的核心 API，就应声明 `min_core_version: "2.6.4"`，避免在旧版核心上运行出错。

---

## 字段声明方式

有两种等价的声明方式，**类属性优先**，manifest 兜底：

### 方式一：manifest.json（推荐，纯元数据）

```json
{
  "slug": "my_plugin",
  "name": "我的插件",
  "version": "1.0.0",
  "author": "我",
  "builtin": false,
  "min_core_version": "2.6.4",
  "requires": ["form", "oss_storage"],
  "extends": "member"
}
```

### 方式二：PluginBase 类属性

```python
from app.plugin_api import PluginBase

class MyPlugin(PluginBase):
    slug = 'my_plugin'
    version = '1.0.0'
    min_core_version = '2.6.4'
    requires = ['form', 'oss_storage']
    extends = 'member'
```

> 若两处同时声明，以类属性为准（`requires` 非空时优先类属性，否则读 manifest）。

---

## 版本比较规则

版本号按 `.` 分段逐段整数比较，缺位以 `0` 补齐：

| A | B | 结果 |
|---|---|------|
| `2.6.3` | `2.6.3` | 相等 |
| `2.6.3` | `2.7.0` | A < B |
| `2.6.3` | `2.6.2` | A > B |
| `2.6` | `2.6.0` | 相等（缺位补 0） |
| `10.1` | `9.9` | A > B（按数字比较，非字符串） |

非数字字符会被截断（如 `2.6.3-beta` 取 `2.6.3`），空字符串视为 `0`。

---

## 启用时强校验

调用 `enable_plugin(slug)` 时，系统按以下顺序校验，**任一不通过即拒绝启用**并返回错误信息（不写入启用清单、不执行种子权限与建表）：

### 1. 最低核心版本

```
当前核心版本 < min_core_version  →  拒绝
```

示例：核心为 `2.6.3`，插件声明 `min_core_version: "2.7.0"` → 拒绝。

### 2. 依赖插件（requires）

对 `requires` 列表中每个 slug 依次检查：

- 该 slug **未安装**（不在插件注册表中）→ 拒绝：「缺少依赖插件：xxx（请先安装）」
- 该 slug **未启用** → 拒绝：「请先启用依赖插件：xxx」

全部已启用则通过。

### 3. 父插件（extends）

- 父插件 **未安装** → 拒绝：「父插件 xxx 未安装，本插件基于其二次开发」
- 父插件 **未启用** → 拒绝：「请先启用父插件 xxx，本插件基于其二次开发」

父插件已安装且已启用则通过。

### 校验顺序

```
min_core_version  →  requires  →  extends
```

前一项不通过则直接返回，不再检查后续项。

---

## 禁用时反向依赖校验

调用 `disable_plugin(slug)` 时，系统会检查**当前已启用的插件**中是否有插件依赖或继承该插件：

- 若存在 → 拒绝禁用：「插件 xxx 被以下已启用插件依赖，无法禁用：yyy, zzz（请先禁用这些插件）」
- 不存在 → 正常禁用

这保证了依赖链的完整性，避免禁用了被依赖的插件导致其他插件运行异常。

### 正确的禁用顺序

若 A `requires: ["B"]`，禁用时需**先禁用 A，再禁用 B**。

---

## 启动期父插件存在性校验

应用启动时（`discover_and_load`），系统会扫描所有插件的 `extends` 字段：

- 若父插件**未安装**（不在 `plugins/` 目录）→ 该子插件被标记为「加载失败」，错误信息为「父插件 xxx 未安装」，后台管理页标红。
- 不阻断其他插件加载，也不阻断应用启动。

这是一道早期防线，让缺失父插件的问题在启动时就暴露，而不是等到启用时才发现。

---

## 错误信息一览

| 场景 | 错误信息 |
|------|----------|
| 核心版本过低 | `核心版本 2.6.3 低于插件要求的最低版本 2.7.0，请先升级 CMS` |
| 依赖插件未安装 | `缺少依赖插件：form（请先安装）` |
| 依赖插件未启用 | `请先启用依赖插件：form` |
| 父插件未安装 | `父插件 form 未安装，本插件基于其二次开发` |
| 父插件未启用 | `请先启用父插件 form，本插件基于其二次开发` |
| 禁用被依赖插件 | `插件 form 被以下已启用插件依赖，无法禁用：banner（请先禁用这些插件）` |

---

## 后台展示

后台「插件管理」列表页在插件说明下方以徽章形式展示依赖信息：

- 🔵 **`≥ 2.6.3`**（蓝）：最低核心版本
- 🟡 **`form, oss_storage`**（黄）：依赖插件
- 🟣 **`member`**（紫）：父插件

无依赖/继承/版本声明的插件不显示徽章。

---

## 完整示例

### 场景：基于 form 插件二次开发的高级表单插件

```json
{
  "slug": "form_advanced",
  "name": "高级表单",
  "version": "1.0.0",
  "description": "基于自定义表单插件二次开发，增加条件分支与工作流",
  "author": "社区贡献",
  "builtin": false,
  "min_core_version": "2.6.4",
  "requires": ["form"],
  "extends": "form"
}
```

**启用时校验过程**：

1. 核心版本 `2.6.4` ≥ `2.6.4` ✓
2. `form` 已安装且已启用 ✓
3. `form` 已安装且已启用 ✓
4. 校验通过，启用成功

**若 `form` 未启用**：返回「请先启用依赖插件：form」，同时「请先启用父插件 form」（因 requires 先校验，实际先返回 requires 的错误）。

**禁用 `form` 时**：因 `form_advanced` 依赖并继承 `form`，返回「插件 form 被以下已启用插件依赖，无法禁用：form_advanced」。需先禁用 `form_advanced`，再禁用 `form`。

### 场景：纯依赖（无继承）

```json
{
  "slug": "analytics_plus",
  "name": "增强统计",
  "version": "1.0.0",
  "requires": ["analytics", "oss_storage"]
}
```

启用前需先启用 `analytics` 和 `oss_storage`。

---

## 注意事项

1. **依赖必须已启用**，仅安装不够。这是因为依赖插件的蓝图、模板全局函数等只有在启用后才会注册到运行时。
2. **不支持循环依赖**。若 A `requires: ["B"]` 且 B `requires: ["A"]`，两者都无法启用（任一启用时另一个未启用）。请重构插件结构消除循环。
3. **`extends` 表示代码层面的二次开发**，通常也应在 `requires` 中声明同一父插件（运行时仍需父插件启用）。两者可同时存在，校验会分别检查。
4. **禁用插件不会自动禁用其依赖**。系统只阻止禁用「被依赖」的插件，不阻止禁用「有依赖」的插件（因为其依赖可能还被其他插件使用）。
5. **现有插件无需改动**。未声明新字段的插件校验全部跳过，行为与 v2.6.4 之前完全一致。
